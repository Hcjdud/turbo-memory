# Используем более новую версию aiogram (совместима с Python 3.14)
aiogram==3.17.0
# Позволяем pip выбрать любую версию pydantic из диапазона, который требует aiogram
pydantic>=2.4.0,<2.10.0
# Остальные зависимости с проверенными версиями
aiohttp==3.10.11
requests==2.32.3
python-dotenv==1.0.1
starlette==0.37.2
uvicorn==0.30.6
